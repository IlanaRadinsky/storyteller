<h1>Create new mediafile page</h1>

<%= form_for @mediafile, url: {action: "create"} do |f| %>
	 <%= f.label :title %>
	 <%= f.text_field :title %>
	 <%= f.label :description %>
	 <%= f.text_area :description %>
	 <%= f.submit "Create Media" %>
<% end %>
